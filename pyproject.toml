[project]
name = "air-co2-enclosure"
version = "0.1.0"
description = "CadQuery 模型：air-co2-carrier-v1 上下分体外壳（Bambu P1S 打印）"
requires-python = ">=3.10"
dependencies = [
    "cadquery>=2.4",
]

[project.scripts]
airco2-report = "scripts.report:main"
airco2-export = "scripts.export_stl:main"

[tool.uv]
package = true
